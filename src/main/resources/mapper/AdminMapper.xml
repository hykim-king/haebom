<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.pcwk.ehr.admin.AdminMapper">
	<select id="getMemberRegistMonth" parameterType="map" resultType="map">
	    SELECT
	        M.MONTH,
	        NVL(COUNT(U.USER_NO),0) AS CNT
	    FROM (
	        SELECT TO_CHAR(LEVEL,'FM00') AS MONTH
	        FROM DUAL
	        <![CDATA[ CONNECT BY LEVEL <= 12   ]]>
	    ) M
	    LEFT JOIN SS_USER U
	        ON SUBSTR(U.USER_REG,1,4) = #{year}
	       AND SUBSTR(U.USER_REG,5,2) = M.MONTH
	    GROUP BY M.MONTH
	    ORDER BY M.MONTH
	</select>

    <select id="getMemberRegistDay" parameterType="map" resultType="map">
	    SELECT
	        D.DAY,
	        NVL(COUNT(U.USER_NO), 0) AS CNT
	    FROM (
	        SELECT TO_CHAR(LEVEL, 'FM00') AS DAY
	        FROM DUAL
	        <![CDATA[ CONNECT BY LEVEL <= TO_NUMBER(   ]]>
	            TO_CHAR(
	                LAST_DAY(
	                    TO_DATE(#{year} || LPAD(#{month}, 2, '0'), 'YYYYMM')
	                ),
	                'DD'
	            )
	        )
	    ) D
	    LEFT JOIN SS_USER U
	        ON SUBSTR(U.USER_REG, 1, 4) = #{year}
	       AND SUBSTR(U.USER_REG, 5, 2) = LPAD(#{month}, 2, '0')
	       AND SUBSTR(U.USER_REG, 7, 2) = D.DAY
	    GROUP BY D.DAY
	    ORDER BY D.DAY
	</select>

	<select id="getReportCount" parameterType="map" resultType="map">
		SELECT
			CASE REP_CLSF
				WHEN 10 THEN '욕설'
				WHEN 20 THEN '음란'
				WHEN 30 THEN '홍보'
				WHEN 40 THEN '개인정보 유출'
				WHEN 50 THEN '기타'
			END AS TYPE,
			COUNT(*) AS CNT
		FROM REPORT
		WHERE REP_REG BETWEEN #{startDate} AND #{endDate}
		GROUP BY REP_CLSF
		ORDER BY REP_CLSF
	</select>

</mapper>
