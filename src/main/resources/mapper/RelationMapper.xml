<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
 "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

 <mapper namespace="com.pcwk.ehr.relation.RelationMapper">
    <!-- 찜하기 분류 -->
    <update id="FavoriteStatus" parameterType="RelationVO">
    
    UPDATE RELATION
       SET REL_CLSF = 10
     WHERE USER_NO = #{userNo}         
       AND TRIP_CONTS_ID = #{tripContsId} 
    
     </update>

    <!-- 내가 여행한 여행지 분류-->
    <update id="VisitedStatus" parameterType="RelationVO">
    
    UPDATE RELATION
       SET REL_CLSF = 20  
     WHERE USER_NO = #{userNo}
       AND TRIP_CONTS_ID = #{tripContsId}
    
    </update>

 </mapper>